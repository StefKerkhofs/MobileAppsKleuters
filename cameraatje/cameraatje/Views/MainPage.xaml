<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             NavigationPage.HasNavigationBar="False"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="cameraatje.Views.MainPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="100">
            <Button Image="{Binding School}" Command="{Binding NavigateToOverviewToddlerCommand}" WidthRequest="200" HeightRequest="200"/>
            <Button Image="{Binding Home}" Command="{Binding NavigateToLoginCommand}" WidthRequest="200" HeightRequest="200"/>
        </StackLayout>
    </ContentPage.Content>
   

</ContentPage>