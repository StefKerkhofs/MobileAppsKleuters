<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="cameraatje.Views.Login">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="1" Grid.Row="1" Text="E-mail:"/>
            <Entry Grid.Column="1" Grid.Row="2" Placeholder="Jouw e-mail" Text="{Binding Email}"/>
            <Label Grid.Column="1" Grid.Row="2" Text="Wachtwoord:"/>
            <Entry Grid.Column="1" Grid.Row="2" IsPassword="True" Placeholder="Jouw wachtwoord" Text="{Binding Password}"/>
            <Button Grid.Column="1" Grid.Row="3"  Command="{Binding LoginCommand}"  Text="Login"/>
            
        </Grid>
    </ContentPage.Content>
</ContentPage>